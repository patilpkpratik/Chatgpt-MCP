# MCP runtime mode: stdio, streamable-http, or sse
MCP_TRANSPORT=streamable-http

# Network bind configuration
MCP_HOST=127.0.0.1
MCP_PORT=8000

# Transport paths
MCP_STREAMABLE_HTTP_PATH=/mcp
MCP_SSE_PATH=/sse

# Optional NWS User-Agent override
WEATHER_USER_AGENT=weather-app/1.0
